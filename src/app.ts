const app = document.getElementById('gantt')!;
const timeline = app.querySelector('.gantt-timeline')!;

const gridCols = 30;
const gridRows = 9;

document.addEventListener('DOMContentLoaded', () => {
    // Render task pane contents

    // Render grid columns

});
